message Alinea2 {
    optional group query1 (MAP) {
        repeated group entry_query1{
            required binary key (STRING);
            required binary value (STRING);
        }

    }
    optional group query2 (MAP) {
        repeated group entry_query2{
            required binary key (STRING);
            required binary value (STRING);
        }

     }


}